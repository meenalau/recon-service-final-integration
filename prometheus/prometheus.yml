global:
  scrape_interval: 15s

scrape_configs:

  - job_name: 'recon-service'

    metrics_path: '/actuator/prometheus'

#    static_configs:
 #    - targets:
  #        - 'localhost:8080'
# this code is without docker

#if we need it with docker  later see in sheet Day5 prometheus
#for docker

    static_configs:
      - targets:
          - 'recon-service:8080'